{% block body %}
    <h1>{{  'template.technician.index.h1' | trans }}</h1>

    <table class="table">
        <thead>
        <tr>
            <th>{{ 'template.technician.index.salary' | trans }}</th>
            <th>{{ 'template.technician.index.formation' | trans }}</th>
            <th>{{ 'template.technician.index.experience' | trans }}</th>
            <th>{{ 'template.technician.index.productivity' | trans }}</th>
        </tr>
        </thead>
        <tbody>
        {% for researcher in researchers %}
            <tr>
                <td>{{ researcher.salary }}</td>
                <td>{{ researcher.formation }}</td>
                <td>{{ researcher.exprience }}</td>
                <td>{{ researcher.productivity }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">{{ 'template.technician.index.record' | trans }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
