<form method="post" action="{{ path('production_lign_delete', {'id': production_lign.id}) }}" onsubmit="return confirm({{ 'button.confirm.delete.productionlign' | trans }});">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ production_lign.id) }}">
    <button class="btn">{{ 'button.delete' | trans }}</button>
</form>
