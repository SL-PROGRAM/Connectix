{% block body %}
    <h1>{{  'template.salesdirector.index.h1' | trans }}</h1>

    <table class="table">
        <thead>
        <tr>
            <th>{{ 'template.salesdirector.index.salary' | trans }}</th>
            <th>{{ 'template.salesdirector.index.formation' | trans }}</th>
            <th>{{ 'template.salesdirector.index.experience' | trans }}</th>
            <th>{{ 'template.salesdirector.index.productivity' | trans }}</th>
        </tr>
        </thead>
        <tbody>
        {% for researcher in researchers %}
            <tr>
                <td>{{ researcher.salary }}</td>
                <td>{{ researcher.formation }}</td>
                <td>{{ researcher.exprience }}</td>
                <td>{{ researcher.productivity }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">{{ 'template.salesdirector.index.record' | trans }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
