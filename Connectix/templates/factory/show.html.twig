{% extends 'base.html.twig' %}

{% block title %}Factory{% endblock %}

{% block body %}
    <h1>Factory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ factory.id }}</td>
            </tr>
            <tr>
                <th>TurnCreation</th>
                <td>{{ factory.turnCreation }}</td>
            </tr>
            <tr>
                <th>CreationCost</th>
                <td>{{ factory.creationCost }}</td>
            </tr>
            <tr>
                <th>MaintenanceCost</th>
                <td>{{ factory.maintenanceCost }}</td>
            </tr>
            <tr>
                <th>AdministrationCost</th>
                <td>{{ factory.administrationCost }}</td>
            </tr>
            <tr>
                <th>AmortizationTurn</th>
                <td>{{ factory.amortizationTurn }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ factory.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('factory_index') }}">back to list</a>

    <a href="{{ path('factory_edit', {'id': factory.id}) }}">edit</a>

    {{ include('factory/_delete_form.html.twig') }}
{% endblock %}
