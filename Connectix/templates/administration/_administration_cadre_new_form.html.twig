<form method="post" action="{{ path('administration_cadre_new') }}">
    <input type="hidden" name="_method" value="NEW_C">
    <button class="btn btn-sm btn-block">{{ 'template.administration.form.cadre' | trans }}</button>
</form>
