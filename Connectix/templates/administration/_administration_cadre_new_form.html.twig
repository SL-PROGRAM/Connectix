<form method="post" action="{{ path('administration_cadre_new') }}" onsubmit="return confirm('Are you sure you want to recrute a cadre administrative?');">
    <input type="hidden" name="_method" value="NEW_C">
    <button class="btn btn-sm btn-block">New cadre</button>
</form>
