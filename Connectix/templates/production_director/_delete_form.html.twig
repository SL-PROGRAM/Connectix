<form method="post" action="{{ path('production_director_delete', {'id': production_director.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ production_director.id) }}">
    <button class="btn">Delete</button>
</form>
