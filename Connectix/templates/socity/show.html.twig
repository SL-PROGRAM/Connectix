{% extends 'base.html.twig' %}

{% block title %}Socity{% endblock %}

{% block body %}
    <h1>Socity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ socity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ socity.name }}</td>
            </tr>
            <tr>
                <th>PriceMinPublicicyImpact</th>
                <td>{{ socity.priceMinPublicicyImpact }}</td>
            </tr>
            <tr>
                <th>PriceMaxPublicityImpact</th>
                <td>{{ socity.priceMaxPublicityImpact }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('socity_index') }}">back to list</a>

    <a href="{{ path('socity_edit', {'id': socity.id}) }}">edit</a>

    {{ include('socity/_delete_form.html.twig') }}
{% endblock %}
