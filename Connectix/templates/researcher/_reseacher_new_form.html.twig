<form method="post" action="{{ path('researcher_new') }}">
    <input type="hidden" name="_method" value="NEW">
    <button class="btn btn-sm btn-block">{{  'template.researcher.form.new' | trans }}</button>
</form>
