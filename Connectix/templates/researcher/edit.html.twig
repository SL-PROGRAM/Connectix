{% extends 'base.html.twig' %}

{% block title %}Edit Researcher{% endblock %}

{% block body %}
    <h1>Edit Researcher</h1>

    {{ include('researcher/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('researcher_index') }}">back to list</a>

    {{ include('researcher/_delete_form.html.twig') }}
{% endblock %}
